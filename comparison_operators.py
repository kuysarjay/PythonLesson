# Comparison Operators - this compares values with the answer of TRUE or FALSE
    # equal to : ==
    # not equal to : !=
    # greater than : >
    # less than : <
    # greater than or equal to : >=
    # less than or equal to : <=
    
a = 10
b = 20

compare = a == b, a != b, a > b, a < b, a >= b, a <= b

print (compare)